<%= link_to @category.name, category_path(@category), class: 'edit-link' %> > new topic

<h3>New Topic</h3>
 
<div class="row">
  <div class="col-md-4">
    <p>Guidelines for categories:</p>
    <ul>
      <li>Make sure the category is related to coding.</li>
      <li>Never insult dogs.</li>
      <li>Smile when you type.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@category, @topic] do |f| %>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: 'form-control', placeholder: "Enter category name" %>
      </div>
      <div class="form-group">
        <%= f.label :description %>
        <%= f.text_area :description, rows: 4, class: 'form-control', placeholder: "Enter category description" %>
      </div>
      <div class="form-group">
        <%= f.label :parent_topic%>
        <%= f.collection_select(:topic_id, @topics, :id, :name, {:prompt=>false}) %>
      </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>